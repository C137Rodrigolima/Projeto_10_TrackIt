import styled from "styled-components";

const StyledMenu = styled.div`
    width: 100%;
    height: 70px;
    bottom: 0;

    display: flex;
    justify-content: space-between;
    align-items: center;
    position: fixed;

    background: #FFFFFF;
`;

export {StyledMenu};